create table r_stock_price (
	SP_ID INT AUTO_INCREMENT NOT NULL,
    PRIMARY KEY (SP_ID),
    REF_STOCK_ID INT NOT NULL,
    FOREIGN KEY(REF_STOCK_ID)
    REFERENCES t_spare_stocks (STOCK_ID),
    SP_PRICE DECIMAL NOT NULL
)